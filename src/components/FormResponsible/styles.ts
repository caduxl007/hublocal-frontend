import styled from 'styled-components';

export const HeaderContent = styled.header`
  display: flex;
  align-items: center;

  svg {
    width: 1.5rem;
    height: 1.5rem;
    font-weight: normal;
    cursor: pointer;
  }

  h2 {
    margin-left: 20px;
    font-size: 1.5rem;
    line-height: 2rem;
    color: #fff;
    font-weight: 600;
  }
`;
